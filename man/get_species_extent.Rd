% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic-maps.R
\name{get_species_extent}
\alias{get_species_extent}
\title{Get the geographic extent of a species from OBIS}
\usage{
get_species_extent(taxonid, startdate = NULL, enddate = NULL, precision = 2)
}
\arguments{
\item{taxonid}{Integer or character. OBIS taxon identifier.}

\item{startdate}{Character or NULL. ISO 8601 start date filter. Default:
\code{NULL}.}

\item{enddate}{Character or NULL. ISO 8601 end date filter. Default:
\code{NULL}.}

\item{precision}{Integer. H3 resolution used for the grid query (1–15).
Default: \code{2}.}
}
\value{
An \code{sf} object with the occurrence grid polygons.
}
\description{
Queries the OBIS API occurrence grid endpoint and returns an \code{sf}
object that can be passed to \code{fit_bounds()} to zoom a map to the
species range.
}
\examples{
\dontrun{
ext <- get_species_extent(taxonid = 955271)

blank_map(projection = "mercator") |>
  add_species_data(taxonid = 955271) |>
  fit_bounds(ext)
}

}
