% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic-maps.R
\name{maplibre_h3_local}
\alias{maplibre_h3_local}
\title{Create a standalone \code{maplibre_h3} widget without a package install}
\usage{
maplibre_h3_local(
  data,
  h3_column = "h3",
  value_column = "value",
  center = c(0, 25),
  zoom = 2,
  ...
)
}
\arguments{
\item{data}{A data frame with H3 cell index and value columns.}

\item{h3_column}{Character. Column name for H3 indices. Default: \code{"h3"}.}

\item{value_column}{Character. Column name for the numeric value.
Default: \code{"value"}.}

\item{center}{Numeric vector \code{c(longitude, latitude)}. Initial map
centre. Default: \code{c(0, 25)}.}

\item{zoom}{Numeric. Initial zoom level. Default: \code{2}.}

\item{...}{Ignored (reserved for future use).}
}
\value{
An \code{htmlwidget} object.
}
\description{
A lightweight wrapper around \code{htmlwidgets::createWidget()} that wires
up all CDN and local JS/CSS dependencies manually. Intended for development
and local testing when the package is not installed.
}
\examples{
\dontrun{
df <- data.frame(
  h3    = c("8429a1fffffffff", "8429a3fffffffff"),
  value = c(100, 500)
)
w <- maplibre_h3_local(df, center = c(3.5, 55), zoom = 4)
w
htmlwidgets::saveWidget(w, "map.html", selfcontained = TRUE)
}

}
