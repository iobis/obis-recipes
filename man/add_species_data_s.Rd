% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-maps.R
\name{add_species_data_s}
\alias{add_species_data_s}
\title{Add a gridded species occurrence layer to a basemap}
\usage{
add_species_data_s(
  map,
  grid_data,
  limit_by_bbox = TRUE,
  plot_xlim = NULL,
  plot_ylim = NULL,
  map_crs = NA,
  auto_breaks = FALSE,
  breaks = c(1, 10, 100, 1000, 10000),
  colors = c("#2c7bb6", "#abd9e9", "#ffffbf", "#fdae61", "#d7191c"),
  legend = TRUE,
  ...
)
}
\arguments{
\item{map}{A \link[ggplot2:ggplot]{ggplot2::ggplot} basemap, typically from \code{\link[=blank_map_s]{blank_map_s()}} or
\code{\link[=grey_map_s]{grey_map_s()}}.}

\item{grid_data}{An \link[sf:sf]{sf::sf} object with a \code{records} column, as returned by
the grid functions in this package.}

\item{limit_by_bbox}{Logical. If \code{TRUE}, zooms the map to the bounding box
of \code{grid_data}. Default \code{TRUE}.}

\item{plot_xlim}{Numeric vector of length 2, or \code{NULL}. Manual longitude
limits used when \code{limit_by_bbox = FALSE}. Default \code{NULL}.}

\item{plot_ylim}{Numeric vector of length 2, or \code{NULL}. Manual latitude
limits used when \code{limit_by_bbox = FALSE}. Default \code{NULL}.}

\item{map_crs}{Numeric or character CRS, or \code{NA} for WGS 84. Default \code{NA}.}

\item{auto_breaks}{Logical. If \code{TRUE}, uses \code{\link[ggplot2:scale_colour_continuous]{ggplot2::scale_fill_binned()}}
with automatic breaks instead of \code{obis_scale()}. Default \code{FALSE}.}

\item{breaks}{Numeric vector of bin boundaries passed to \code{obis_scale()} and
\code{\link[=prepare_grid_data]{prepare_grid_data()}}. Default \code{c(1, 10, 100, 1000, 10000)}.}

\item{colors}{Character vector of fill colors, one per bin. Default is the
OBIS blue-to-red palette.}

\item{legend}{Logical. Show the fill legend. Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsf]{ggplot2::coord_sf()}}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Overlays a spatial grid of occurrence counts (from \code{\link[=get_occ_gridded]{get_occ_gridded()}},
\code{\link[=get_occ_tiles]{get_occ_tiles()}}, or any of the \code{occ_to_*grid()} converters) onto an
existing ggplot2 basemap.
}
\examples{
\dontrun{
# Geohash grid on a blank map, zoomed to data extent
grid <- get_occ_tiles(taxonid = 141438)
blank_map_s() |> add_species_data_s(grid_data = grid)

# Global view on a grey map with automatic breaks
grey_map_s() |>
    add_species_data_s(
        grid_data   = grid,
        limit_by_bbox = FALSE,
        auto_breaks = TRUE
    )

# Custom breaks and colors, no legend
blank_map_s() |>
    add_species_data_s(
        grid_data = grid,
        breaks    = c(1, 5, 50, 500),
        colors    = c("#f7fbff", "#6baed6", "#2171b5", "#084594"),
        legend    = FALSE
    )
}
}
