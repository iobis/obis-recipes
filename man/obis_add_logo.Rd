% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot-styles.R
\name{obis_add_logo}
\alias{obis_add_logo}
\title{Add OBIS and IOC logos to a ggplot}
\usage{
obis_add_logo(
  plot,
  position = c("bottomright", "bottomleft", "topright", "topleft"),
  logo_size = 0.07,
  padding = 0.015,
  fig_asp = 7/5
)
}
\arguments{
\item{plot}{A ggplot2 object.}

\item{position}{Character. Corner for the logos. One of \code{"bottomright"},
\code{"bottomleft"}, \code{"topright"}, or \code{"topleft"}. Default \code{"bottomright"}.}

\item{logo_size}{Numeric in \verb{(0, 1)}. Height of the IOC logo as a fraction
of the figure height. Default \code{0.07}.}

\item{padding}{Numeric in \verb{(0, 1)}. Margin from the figure edge, as a
fraction of the figure height. Default \code{0.015}.}

\item{fig_asp}{Numeric. Figure width / height ratio used to correct the logo
aspect ratios in normalised coordinates. Default \code{7 / 5}.}
}
\value{
A \code{ggdraw} object that prints and saves like a ggplot.
}
\description{
Overlays the OBIS and IOC logos at a chosen corner of a finished ggplot using
\code{\link[cowplot:ggdraw]{cowplot::ggdraw()}}. The logos are placed side by side matching the style
used on the OBIS website (IOC left, OBIS right).
}
\details{
Because logo width in normalised figure coordinates depends on the physical
aspect ratio of the output, pass \code{fig_asp = width / height} to match the
dimensions you plan to use in \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. The default (\code{7 / 5})
works well for the standard 7 × 5 inch ggplot figure.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
    geom_point(color = "#0072B2", size = 2) +
    labs(title = "Fuel efficiency", x = "Weight (1000 lbs)", y = "MPG") +
    theme_obis()
obis_add_logo(p)
}
