% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot-styles.R
\name{theme_obis_pub}
\alias{theme_obis_pub}
\title{Publication-quality ggplot2 theme for OBIS}
\usage{
theme_obis_pub(
  base_size = 11,
  base_family = "roboto",
  legend_position = "right",
  grid = c("none", "y", "xy")
)
}
\arguments{
\item{base_size}{Numeric. Base font size in pt. Default \code{11}. Reduce to
7–8 pt when exporting at narrow journal column widths (≤ 3.5 in), or use
\code{obis_ggsave()} to handle scaling automatically.}

\item{base_family}{Character. Base font family. Default \code{"roboto"}. Swap for
\code{"helvetica"} or \code{"arial"} if those are available on your system.}

\item{legend_position}{Character or \code{"none"}. Position of the legend.
Default \code{"right"}.}

\item{grid}{Character. Reference lines to draw: \code{"none"} (default, typical
for Nature / Science), \code{"y"} for subtle major y-axis lines only, or
\code{"xy"} for both axes.}
}
\value{
A \link[ggplot2:theme]{ggplot2::theme} object.
}
\description{
A minimal, journal-ready theme following the visual conventions of top
scientific publications (Nature, Science, Cell, One Earth). Key
characteristics: clean L-shaped axes with outward tick marks, no background
grid, and a compact layout suited to single- and multi-panel figures. Call
\code{\link[=obis_load_fonts]{obis_load_fonts()}} once per session to enable the Roboto font.
}
\details{
The default \code{base_size} of \code{11} is comfortable for screen use and for
figures saved at standard dimensions (≈ 7 × 5 in). When targeting the
narrow column widths of print journals (e.g. 3.5 in single-column), either
reduce \code{base_size} to 7–8 or use \code{obis_ggsave()}, which rescales all
elements proportionally to the output dimensions.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) +
    geom_point(size = 1.5) +
    labs(title = "Fuel efficiency", x = "Weight (1000 lbs)", y = "MPG") +
    theme_obis_pub()
}
