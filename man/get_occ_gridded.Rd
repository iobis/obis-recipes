% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-maps.R
\name{get_occ_gridded}
\alias{get_occ_gridded}
\title{Fetch OBIS occurrences and convert to a spatial grid}
\usage{
get_occ_gridded(
  taxonid = 141438,
  startdate = NULL,
  enddate = NULL,
  type_grid = "geohash",
  grid_res = 3
)
}
\arguments{
\item{taxonid}{Integer. OBIS taxon ID. Default \code{141438} (whale shark).}

\item{startdate}{Character or \code{NULL}. Earliest date to include, in
\code{"YYYY-MM-DD"} format. Default \code{NULL}.}

\item{enddate}{Character or \code{NULL}. Latest date to include, in
\code{"YYYY-MM-DD"} format. Default \code{NULL}.}

\item{type_grid}{Character. Grid system to use: \code{"geohash"}, \code{"h3"}, or
\code{"a5"}. Default \code{"geohash"}.}

\item{grid_res}{Integer. Grid resolution passed to the corresponding
converter (\code{\link[=occ_to_geohashgrid]{occ_to_geohashgrid()}}, \code{\link[=occ_to_h3grid]{occ_to_h3grid()}}, or
\code{\link[=occ_to_a5grid]{occ_to_a5grid()}}). Default \code{3}.}
}
\value{
An \link[sf:sf]{sf::sf} object with one row per occupied cell and a \code{records}
column containing the occurrence count.
}
\description{
Retrieves occurrence records from OBIS via \code{\link[robis:occurrence]{robis::occurrence()}} and
aggregates them into a spatial grid using one of three grid systems:
geohash, H3, or A5.
}
\examples{
\dontrun{
# Geohash grid at resolution 3
get_occ_gridded(taxonid = 141438, type_grid = "geohash", grid_res = 3)

# H3 hexagonal grid with date filter
get_occ_gridded(
    taxonid   = 141438,
    startdate = "2010-01-01",
    type_grid = "h3",
    grid_res  = 3
)

# A5 pentagonal grid
get_occ_gridded(taxonid = 141438, type_grid = "a5", grid_res = 3)
}
}
