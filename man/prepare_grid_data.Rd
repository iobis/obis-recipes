% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-maps.R
\name{prepare_grid_data}
\alias{prepare_grid_data}
\title{Bin a numeric grid column into labelled factor levels for plotting}
\usage{
prepare_grid_data(
  grid_data,
  column = "records",
  breaks = c(1, 10, 100, 1000, 10000)
)
}
\arguments{
\item{grid_data}{An \link[sf:sf]{sf::sf} object containing the column to bin.}

\item{column}{Character. Name of the numeric column to bin. Default
\code{"records"}.}

\item{breaks}{Numeric vector of bin boundaries (same as \code{\link[=obis_scale]{obis_scale()}}).
Default \code{c(1, 10, 100, 1000, 10000)}.}
}
\value{
The input \code{grid_data} with an additional \verb{<column>_binned} factor
column.
}
\description{
Cuts a numeric column in an sf grid object into ordered factor bins using
the same break scheme as \code{\link[=obis_scale]{obis_scale()}}. The result is a new column named
\verb{<column>_binned} ready for use with \code{\link[ggplot2:aes]{ggplot2::aes()}}.
}
\examples{
occ <- data.frame(
    decimalLongitude = c(-122.4, -118.2, -87.6, 2.3, 151.2),
    decimalLatitude  = c(37.8, 34.1, 41.9, 48.9, -33.9)
)
grid <- occ_to_geohashgrid(occ)
prepare_grid_data(grid)
}
