% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot-styles.R
\name{guide_colourbar_h}
\alias{guide_colourbar_h}
\title{Horizontal colourbar guide}
\usage{
guide_colourbar_h(
  title = ggplot2::waiver(),
  barwidth = grid::unit(12, "lines"),
  barheight = grid::unit(0.4, "lines"),
  title_position = "top",
  title_hjust = 0.5,
  label_position = "bottom",
  ...
)
}
\arguments{
\item{title}{Character or \code{\link[ggplot2:waiver]{ggplot2::waiver()}}. Guide title. Default
\code{ggplot2::waiver()} (inherits from scale name).}

\item{barwidth}{A \code{\link[grid:unit]{grid::unit()}} object controlling the bar width.
Default \code{grid::unit(12, "lines")}.}

\item{barheight}{A \code{\link[grid:unit]{grid::unit()}} object controlling the bar height.
Default \code{grid::unit(0.4, "lines")}.}

\item{title_position}{Character. Where to place the title relative to the
bar. One of \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}. Default \code{"top"}.}

\item{title_hjust}{Numeric in \verb{[0, 1]}. Horizontal justification of the
title. Default \code{0.5} (centered).}

\item{label_position}{Character. Where to place tick labels. One of
\code{"top"}, \code{"bottom"}. Default \code{"bottom"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:guide_colourbar]{ggplot2::guide_colorbar()}}.}
}
\value{
A ggplot2 guide object.
}
\description{
Returns a \code{\link[ggplot2:guide_colourbar]{ggplot2::guide_colorbar()}} pre-configured as a thin horizontal
bar, styled similarly to matplotlib's default colorbar. Intended for use
with continuous fill or color scales via the \code{guide} argument or
\code{\link[ggplot2:guides]{ggplot2::guides()}}.
}
\examples{
library(ggplot2)
ggplot(faithfuld, aes(waiting, eruptions, fill = density)) +
    geom_tile() +
    scale_fill_obis_cont("thermal") +
    guides(fill = guide_colourbar_h(title = "Density")) +
    theme_obis(legend_position = "bottom")
}
