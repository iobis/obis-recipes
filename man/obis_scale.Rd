% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-maps.R
\name{obis_scale}
\alias{obis_scale}
\title{OBIS-style binned color scale for ggplot2}
\usage{
obis_scale(
  breaks = c(1, 10, 100, 1000, 10000),
  colors = c("#2c7bb6", "#abd9e9", "#ffffbf", "#fdae61", "#d7191c"),
  legend_title = "Records",
  na_color = "grey80",
  key_spacing = 4
)
}
\arguments{
\item{breaks}{Numeric vector of bin boundaries. The last value is used as
the label for the open-ended top bin. Default \code{c(1, 10, 100, 1000, 10000)}.}

\item{colors}{Character vector of hex colors, one per bin. Must be the same
length as \code{breaks}. Default is a blue-to-red diverging palette.}

\item{legend_title}{Character. Title shown above the legend. Default
\code{"Records"}.}

\item{na_color}{Character. Fill color for \code{NA} values. Default \code{"grey80"}.}

\item{key_spacing}{Numeric. Vertical spacing between legend keys in points.
Default \code{4}.}
}
\value{
A list of two ggplot2 objects that can be added to a plot with \code{+}.
}
\description{
Returns a list of two ggplot2 components — a \code{\link[ggplot2:scale_manual]{ggplot2::scale_fill_manual()}}
with labelled break bins and a \code{\link[ggplot2:theme]{ggplot2::theme()}} that styles the legend —
matching the visual style used on the OBIS taxon pages.
}
\examples{
\dontrun{
grey_map_s() |>
    add_species_data(grid_data = get_occ_tiles(141438)) +
    obis_scale(breaks = c(1, 10, 100, 1000, 10000))
}
}
