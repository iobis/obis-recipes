% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot-styles.R
\name{obis_ggsave}
\alias{obis_ggsave}
\title{Save a ggplot with proportionally scaled text}
\usage{
obis_ggsave(
  filename,
  plot = ggplot2::last_plot(),
  width = 7,
  height = 5,
  ref_width = 7,
  ref_height = 5,
  units = "in",
  dpi = 300,
  bg = "white",
  ...
)
}
\arguments{
\item{filename}{File path for the output (extension determines format).}

\item{plot}{A ggplot2 (or cowplot) object. Defaults to the last plot
displayed (\code{\link[ggplot2:get_last_plot]{ggplot2::last_plot()}}).}

\item{width, height}{Numeric. Target output dimensions in \code{units}.}

\item{ref_width, ref_height}{Numeric. Reference dimensions at which the plot
was designed. Defaults match \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} defaults (\code{7} × \code{5} in).
Adjust if you authored the plot at a different preview size.}

\item{units}{Character. Units for \code{width}, \code{height}, \code{ref_width}, and
\code{ref_height}. One of \code{"in"} (default), \code{"cm"}, \code{"mm"}, or \code{"px"}.}

\item{dpi}{Numeric. Output resolution. Default \code{300} (publication quality).}

\item{bg}{Character. Background colour. Default \code{"white"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
The \code{filename} path, invisibly (same as \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}).
}
\description{
A wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} that keeps all theme elements (text,
lines, points, legend keys) at the same \emph{apparent} size regardless of the
output dimensions. It works by rendering the plot at a reference size and
then letting \code{ggsave} scale the raster to the target dimensions, so a figure
intended for a 3.5 × 2.5 in journal column looks identical whether saved at
that size or at 7 × 5 in for a slide.
}
\details{
The scaling factor is computed as
\eqn{\sqrt{(w \times h) / (w_\text{ref} \times h_\text{ref})}}{sqrt((w*h)/(ref_w*ref_h))},
the geometric mean of the area ratio, so both dimensions are treated equally.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
    geom_point(size = 1.5) +
    labs(x = "Weight (1000 lbs)", y = "MPG") +
    theme_obis_pub()

# Single-column journal figure (3.5 × 2.5 in) — text scales down
obis_ggsave("fig1_single.pdf", p, width = 3.5, height = 2.5)

# Double-column figure (7 × 5 in) — same apparent text size
obis_ggsave("fig1_double.pdf", p, width = 7, height = 5)

# Poster panel (14 × 10 in) — text scales up
obis_ggsave("fig1_poster.pdf", p, width = 14, height = 10)
}
}
