% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-maps.R
\name{grey_map_s}
\alias{grey_map_s}
\title{Create a grey world basemap with optional bathymetry}
\usage{
grey_map_s(
  map_scale = "small",
  map_crs = NA,
  background_color = "#dbdbdc",
  land_background = "white",
  land_line_color = NA,
  add_bathymetry = TRUE,
  bathymetry_color = "#044062",
  bathymetry_opacity = 0.06,
  plot_xlim = NULL,
  plot_ylim = NULL,
  keep_graticule = FALSE,
  ...
)
}
\arguments{
\item{map_scale}{Character. Natural Earth dataset scale. One of \code{"small"},
\code{"medium"}, or \code{"large"}. Default \code{"small"}.}

\item{map_crs}{Numeric or character CRS, or \code{NA} for WGS 84. Default \code{NA}.}

\item{background_color}{Character. Fill color for the ocean/panel background.
Default \code{"#dbdbdc"}.}

\item{land_background}{Character. Fill color for land polygons.
Default \code{"white"}.}

\item{land_line_color}{Character or \code{NA}. Outline color for land polygons.
Default \code{NA} (no outline).}

\item{add_bathymetry}{Logical. If \code{TRUE}, overlays the bundled \code{bath_contour}
dataset as a semi-transparent layer. Default \code{TRUE}.}

\item{bathymetry_color}{Character. Color for bathymetry contour lines.
Default \code{"#044062"}.}

\item{bathymetry_opacity}{Numeric in \verb{[0, 1]}. Opacity of the bathymetry
layer. Default \code{0.06}.}

\item{plot_xlim}{Numeric vector of length 2, or \code{NULL}. Longitude limits
passed to \code{\link[ggplot2:ggsf]{ggplot2::coord_sf()}}. Default \code{NULL}.}

\item{plot_ylim}{Numeric vector of length 2, or \code{NULL}. Latitude limits
passed to \code{\link[ggplot2:ggsf]{ggplot2::coord_sf()}}. Default \code{NULL}.}

\item{keep_graticule}{Logical. Retain axis labels and graticule lines.
Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsf]{ggplot2::coord_sf()}}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws a ggplot2 world map styled after the OBIS taxon page: grey ocean
background, white land, and an optional semi-transparent bathymetry overlay.
}
\examples{
grey_map_s()
grey_map_s(add_bathymetry = FALSE)
grey_map_s(plot_xlim = c(-30, 40), plot_ylim = c(35, 70))
}
